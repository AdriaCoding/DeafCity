Status: ready-for-human

# Design player chrome and navigation

## Parent

[PRD](../PRD.md) — Preview site v3

## What to build

Produce mockups or annotated wireframes for the Preview player chrome and site navigation, sufficient to implement without further product questions. Decisions needed:

1. **Home navigation** — how visitors switch between the player home, About, and Participants (and later Tags, Map) on a **non-scrollable** full-viewport home page
2. **Subtitle language picker placement** — where the Subtitle language control sits in the player chrome (transport row, filter row, near captions, etc.)
3. **Participants grid labels** — show Participant name under each thumbnail, or image-only

Deliverable: design artifacts linked or stored in-repo, referenced by implementation issues 06, 07, and 08.

## Acceptance criteria

- [x] Home navigation pattern is specified for a no-scroll player viewport (PRD D9–D10)
- [x] Secondary page back-navigation pattern is specified (PRD D11)
- [ ] Subtitle language picker placement is specified
- [ ] Participants grid label treatment is specified
- [x] Designs use Roboto, `rgb(0, 120, 0)`, and minimalistic style from the v3 spec *(home + About nav shipped)*

## Blocked by

None — can start immediately

## Comments

> *This was generated by AI during triage.*

### Locked — home & secondary navigation (2026-06-21)

**Home (D9–D10) — implemented:**

- Home page is **non-scrollable** (`overflow: hidden` on document + player root)
- **No top nav bar**
- Site links sit **below** transport controls and language filters, inside player chrome
- Only **other routes** appear — no current-page button (no redundant “Player” on home)
- Today: single **[About]** button, same visual class as secondary transport controls
- Future routes (Participants, Tags, Map) join the same chrome row as they ship (issue 08+)

**Secondary pages (D11) — implemented on About:**

- One link only — not a button row
- Label: **go back to player** with ← prefix
- Text link styling (brand green, underline on hover) — not button chrome
- Participants page should reuse the same pattern when issue 08 ships

**Still open:** subtitle language picker placement (blocks issue 07), Participants grid labels (blocks issue 08 polish).

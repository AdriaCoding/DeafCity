# Secrets live here (config.php, google-sheets-service-account.json).
# Nothing under config/ is ever served over HTTP; PHP includes it from disk.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>

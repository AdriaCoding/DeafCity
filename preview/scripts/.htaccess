# CLI-only scripts. HTTP hits were eating all PHP-FPM workers and taking the site down.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
